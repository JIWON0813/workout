type Query{
    users : [User]
    getUser(name : String) : User
}

type Mutation{
    saveUser(user : UserInput) : Boolean
    quitUser(id : String) : Boolean
    sendMessageToken(message : MessageRequest) : Boolean
}