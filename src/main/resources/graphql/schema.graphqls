scalar DateTime

type Query{
    users : [User]
    user(id : String) : User
    records : [Record]
}

type Mutation{
    createUser(user : UserInput) : Boolean
    quitUser(id : String) : Boolean

    sendMessageToken(message : FcmMessageInput) : Boolean

    addRecord(record : RecordInput) : String
}