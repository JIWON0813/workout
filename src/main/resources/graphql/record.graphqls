input RecordInput{
    recordId : Int
    startTime : DateTime!
    endTime : DateTime!
    record : Int!
}

type Record{
    id : Int
    year : Int
    month : Int
    day : Int
    startTime : DateTime
    endTime : DateTime
    value : Int
}

type TimeStampResponse{
    id : Int
    record : Int
    seed : Int
}
